{
    "name": "entas",
    "version": "1.0.0",
    "scripts": {
        "format": "prettier --write \"**/*.ts\""
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "api": "workspace:*"
    }
}
