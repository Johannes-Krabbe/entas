{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "generate": "prisma format && prisma generate"
  },
  "dependencies": {
    "hono": "^3.11.11"
  },
  "devDependencies": {
    "bun-types": "^1.0.7",
    "prisma": "^5.7.1"
  }
}
